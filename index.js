/*const menuItems = [
  { name: "Espresso", price: "₱120", image: "images/coffee1.jpg" },
  { name: "Caramel Latte", price: "₱160", image: "images/coffee2.jpg" }
];

const container = document.querySelector(".menu-items");

menuItems.forEach(item => {
  container.innerHTML += `
    <div class="menu-item">
      <img src="${item.image}" alt="${item.name}">
      <div class="menu-info">
        <h4>${item.name}</h4>
        <p>${item.price}</p>
      </div>
    </div>
  `;

});*/
